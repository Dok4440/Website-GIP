/* CSS voor index.html, diensten.html, contact.html, de navigatiebalk, footer, knoppen op alle pagina's en scherm veranderingen. */
/* ######################################################## */
/* 						INDEX.HTML 							
/* ######################################################## */

html {
    background-color: #272727;
	color: white;
    font-family: system-ui, Helvetica, Arial, sans-serif, "Hiragino Kaku Gothic ProN";
    font-weight: 100;
}
a { text-decoration: none; color: white; }

.logo-achtergrond {
    position:absolute;
    inset: 0px;
    margin: 30% auto;
	width: 50%;
}

/* qoutes op de hoofdpagina */
blockquote { display: none; }

#quote-een { padding-top: 10%; padding-left: 8% }
#quote-twee { padding-top: 0%; padding-left: 60%; }

/* "over" aan de onderkant van de hoofdpagina. */
#index-over { 
  background: #34374b; 
  width: 90%; 
  text-align: center; 
  margin: 100% auto 10%;
  padding-top: 10px;
  padding-bottom: 30px;
}

#index-over h2 { text-transform: capitalize; }
#index-over p  { padding: 0 15%; }











/* ######################################################## */
/* 						DIENSTEN.HTML 						
/* ######################################################## */

.prijs-tabel {
  margin: 7% auto 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
  column-gap: 20px;
  width: 95%;
  max-width: 100%;
  display: absolute;  
}


.prijs-tabel-pakket {
  border-radius: 4px;
  background: #979797;
  color: black;
  font-weight: 500;
  box-shadow: 0 5px 20px;
  overflow: hidden;
  position: relative;
  margin-top: 25%;
}


.prijs-tabel-header {
    margin: 0;
    padding: 1rem;
    font-size: 1.2em;
    text-align: center;
    font-weight: bold;
    color: black;
    position: relative;
}

#pakket-brons  { background: #E4953C; }
#pakket-zilver { background: #BBC2CC; }
#pakket-goud   { background: #FAE481; }

.pakket-prijs {
    padding: 10px 1rem;
    background: #58595B;
    color: white;
	font-size: 1.2em;
    font-weight: normal;
    text-align: center;
    box-shadow: inset 0 10px 4px -6px rgba(0, 0, 0, 0.1);
} 

.pakket-prijs-groot { 
	display: inline; 
	font-size:350%; 
	font-weight: 500; 
}

.pakket-list {
    list-style: none;
    padding: 0 0 5% 0;
	padding-top: 30px;
    margin: 0;
}
  
.pakket-list-item {
    padding: 0.8em 1em;
    font-size: 90%;
} 

.pakket-list li:nth-child(odd) { background: #808080; }
  
  
.tabel-populair-banner {
    background: #666699;
    transform: rotate(-45deg); /* 45 graden rotatie */
    position: absolute;
    z-index: 1;
    padding: 0.5rem 4rem;
    top: 2rem;
    left: -4rem;
    border: 2px solid #536878;
}

.diensten-knop {
  position:relative;
  margin: 2% auto;
  width: 80%;
}

.diensten-knop:hover a { color:black; }

#diensten-voetnoot {
	padding-top: 5%;
  padding-bottom: 10%;
	width: 95%;
	padding-left: 5.5%;
	font-style: italic;
}











/* ######################################################## */
/* 						CONTACT.HTML 						
/* ######################################################## */

#gegevens-tabel {
  display:fixed;
  border-collapse: collapse;
  width: 100%;
  margin: 25% auto 0;
  font-size: 85%;
}

.gegevens-tabel-data {
  border: 0;
  text-align: left;
  padding: 1%;
} tr:nth-child(even) { background: #a0a0a0; color: black; }


#voetnoot-tabel {
  padding-top: 10px;
  width: 65%;
  margin: auto 0;
  font-style: italic;
}

.contact-formulier {
  width: 95%;
  display:fixed;
  margin: 10% auto;
}

.formulier-titel { 
  font-size: 36px; 
}

.formulier-ondertitel {
  font-size: 16px;
  margin-top: 10px;
}

.input-kader1 { margin-top: 30px; height: 50px; position: relative; }
.input-kader2 { margin-top: 30px; height: 50px; position: relative; }
.input-kader3 { margin-top: 30px; height:200px; position: relative; }

.input {
  background-color: #34374b;
  border-radius: 10px;
  border: 0;
  box-sizing: border-box;
  color: #eee;
  font-size: 18px;
  height: 100%;
  outline: 0;
  padding-right: 20px;
  padding-left: 20px;
  width: 100%;
  resize: none;
} .input2 { padding-top: 15px; } 
 

.lange-dash {
  opacity: 0;
  transition: opacity 200ms;
  position: relative;
  bottom: 77px;
} .lange-dash-bericht { bottom: 227px; }


.placeholder {
  color: #65657b;
  left: 20px;
  line-height: 10px;
  pointer-events: none;
  position: absolute;
  transform-origin: 0 50%;
  transition: transform 200ms, color 200ms;
  top: 20px;
}

#sumbit_knop {
  position:relative;
  width: 100%;
  margin-top: 30px;
  background-color: coral;
} #sumbit_knop:hover .circle { width: 100%; }
  #sumbit_knop:hover { background: transparent; }


.input:focus ~ .lange-dash,
.input:not(:placeholder-shown) ~ .lange-dash { opacity: 1; }
.input:focus ~ .placeholder, .input:not(:placeholder-shown) ~ .placeholder { transform: translateY(-40px) translateX(0px) scale(1); }
.input:not(:placeholder-shown) ~ .placeholder { color: #808097; }
.input:not(:placeholder-shown) ~ .lange-dash { color: #808097; } 
.input:focus ~ .placeholder { color: white; }


/* SPECIALE a'S */
#checklist a, .over-a {
	text-decoration: none;
	color: coral;
}

#checklist a:hover, .over-a:hover, .gegevens-tabel-a:hover, .gegevens-tabel-a2:hover {
	color: #fff;
	background-color: coral;
	text-decoration: none;
}

.gegevens-tabel-a { text-decoration: underline; }
.gegevens-tabel-a2 { color: black; text-decoration: underline; }
/**/


/*CHECKBOX CODE GENOMEN VAN https://codepen.io/kathykato/pen/xxGObEp */

 #checklist {--text: #fff;--check: #4f29f0;--disabled: #c3c8de;--width: 100px;--height: 140px;--border-radius: 10px;position: relative;box-shadow: 0 10px 30px rgba(65, 72, 86, 0.05);padding: 25px 0 0px;display: grid;grid-template-columns: 30px auto;align-items: center;}

 #checklist label {color: var(--text);position: relative;cursor: pointer;align-items: center;width: fit-content;transition: color 0.3s ease;}
 #checklist label::before, #checklist label::after {content: "";position: absolute;margin-top: 10px;}
 #checklist label::before {height: 3px;width: 15px;left: -27px;background: var(--check);border-radius: 2px;transition: background 0.3s ease;}
 #checklist label:after {height: 8px;width: 8px;top: 8px;left: -25px;border-radius: 50%;}

 #checklist input[type="checkbox"] {-webkit-appearance: none;-moz-appearance: none;position: relative;height: 10px;width: 20px;outline: none;border: 0;margin: 0;cursor: pointer;display: grid;align-items: center;}
 #checklist input[type="checkbox"]::before, #checklist input[type="checkbox"]::after {content: "";position: absolute;height: 2px;top: auto;background: var(--check);border-radius: 2px;}
 #checklist input[type="checkbox"]::before {width: 0px;right: 60%;transform-origin: right bottom;}
 #checklist input[type="checkbox"]::after {width: 0px;left: 40%;transform-origin: left bottom;}
 #checklist input[type="checkbox"]:checked::before { animation: check-01 0.4s ease forwards; }
 #checklist input[type="checkbox"]:checked::after { animation: check-02 0.4s ease forwards; }
 #checklist input[type="checkbox"]:checked + label { color: var(--disabled); animation: move 0.3s ease 0.1s forwards; }
 #checklist input[type="checkbox"]:checked + label::before { background: var(--disabled); animation: slice 0.4s ease forwards; }
 #checklist input[type="checkbox"]:checked + label::after { animation: firework 0.5s ease forwards 0.1s; }
 
 @keyframes move { 50% { padding-left: 8px; padding-right: 0px; } 100% { padding-right: 4px; }}
 @keyframes slice { 60% { width: 100%; left: 4px; } 100% { width: 100%; left: -2px; padding-left: 0; }}
 @keyframes check-01 { 0% {width: 4px; top: auto; transform: rotate(0); } 50% { width: 0px; top: auto; transform: rotate(0); } 51% { width: 0px; top: 8px; transform: rotate(45deg); } 100% { width: 5px; top: 8px; transform: rotate(45deg); }}
 @keyframes check-02 { 0% {width: 4px;top: auto;transform: rotate(0);}50% {width: 0px;top: auto;transform: rotate(0);}51% {width: 0px;top: 8px;transform: rotate(-45deg);}100% {width: 10px;top: 8px;transform: rotate(-45deg);}}
 
 /* EINDE CODE GENOMEN VAN https://codepen.io/kathykato/pen/xxGObEp */ 
 








/* ######################################################## */
/*            OPDRACHTEN.HTML
/* ######################################################## */

p {
  margin: 0 0 13px 0;
}

.info {
  max-width: 100%;
  margin: 35% auto 0;
}

.info-stuk {
  display: grid;
  grid-template-columns: 1fr 10fr;
  position: relative;
}

.info-stuk-nummer {
  font-size: 10vw;
  font-weight: 600;
  padding: 0px 5px;
  position: sticky;
  top: 0;
  text-align: center;
  height: calc(10vw + 20px);
  transition: all 0.2s ease-in;
}

.info-stuk-inhoud {
  border-top: 2px solid #fff;
  display: grid;
  grid-gap: 20px;
  padding: 15px 0;
}

.info-stuk-inhoud .info-stuk-titel {
  font-weight: 600;
}

.info-stuk-inhoud .info-stuk-tekst {
  line-height: 26px;
}

.info-link{ color: lightblue; text-decoration: underline; }
.info-link:hover{ color: #fddb3a; }

.naast-elkaar{ float:left; }
#naast-link { margin: 0px 10px 0px; }
 








/* ######################################################## */
/*            OPDRACHTEN.HTML
/* ######################################################## */
 
#privpol {
  display:fixed;
  border-collapse: collapse;
  width: 100%;
  margin: 25% auto 0;
  font-size: 100%;
}	








/* ######################################################## */
/* 						NAVIGATIEBALK 						
/* ######################################################## */

.navigatie-menu {
  position: absolute;
  top: 7%;
  left:50%;
  width: 100%;
  word-spacing: 10px;
  margin: 5% auto 10%; 
  transform: translate(-50%, -50%);
  text-align: center;
}

.navigatie-menu a {
  color: #f6f4e6;
  text-decoration: none;
  font-size: 90%;
  text-transform: uppercase;
  display: inline;
  font-weight: 500;
  width: 80px;
  transition: 200ms;
}

.navigatie-menu a:hover {
  color: #fddb3a;	
}

.navigatie-menu .huidige-pagina{
	color: #b58b00;
}

.talen-menu .huidige-taal{
	background-color: #b58b00;
}

.helpdesk{
	display: none;
	margin-top: -25px;
	float:right;
	padding: 5px;
}
 
#hd-button{
	position:relative;
	margin: 2% auto;
	width: 100%;
	color:black;
}









/* ######################################################## */
/* 							FOOTER						
/* ######################################################## */
footer ul {
  display: inline-grid;
  grid-auto-flow: row;
  grid-gap: 24px;
  justify-items: center;
  margin: auto;
  list-style-type: none;
}

footer a {
  color: white;
  text-decoration: none;
  box-shadow: inset 0 -1px 0 hsla(0, 0%, 100%, 0.4);
}

footer a:hover {
  box-shadow: inset 0 -1.2em 0 hsla(0, 0%, 100%, 0.4);
}

footer li:last-child {
  grid-column: 1 / 2;
  grid-row: 1 / 2;
}

footer li:hover ~ li p {
  animation: wuiven-animatie 0.3s infinite;
}

footer {
  display: flex;
  height: 100px;
  width: 100%;
  line-height: 1.3;
  font-family: Menlo, monospace;
}

@keyframes wuiven-animatie {
  0%,
  100% { transform: rotate(0deg);}
  25% { transform: rotate(20deg);}
  75% { transform: rotate(-15deg);}
}











/* ######################################################## */
/* 							KNOPPEN 						
/* ######################################################## */

button {
    background: #34374b;
    border-radius: 12px;
    border: 0;
    box-sizing: border-box;
    color: #eee;
    cursor: pointer;
    font-size: 18px;
    height: 50px;
    outline: 0;
    text-align: center;
    width: 320px;
    display: flex;
    align-items: center;
    padding: 20px;
    justify-content: center;

    transition: 1000ms;
    -webkit-tap-highlight-color: transparent;
}
  
button .circle {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    position: absolute;
    top: 0;
    left: 50%;
    overflow: hidden;
    transition: 500ms;
}
  
button:hover { background: transparent; }
  
button:hover .circle {
    height: 50px;
    width: 320px;
    left: 0;
    border-radius: 0;
    border-bottom: 2px solid black;
}

#hd-button{
	background: #9ae2d5;
}

#hd-button:hover {background: transparent; color: white; }









/* ######################################################## */
/* 							TANKS LMAO						
/* ######################################################## */

#TANKSdesktopClass{
	visibility: hidden;
}

#TANKSmobileClass{
	margin-top: 50%;
	text-align: center;
}











/* ######################################################## */
/*            SCHERM VERANDERINGEN
/* ######################################################## */

/*de css is geschreven in 'mobile-first', hier zijn kleine veranderingen voor grotere schermen. */
@media only screen and (min-width: 768px) {
	
	.logo-achtergrond {
		margin: 5% auto;
		width: 300px;
	}
	
	blockquote {
		display: flex;
		font-family: 'EB Garamond', serif;
		font-size: 200%;
	}
	
	#index-over { 
		margin: 7% auto 0;  
	}

	.prijs-tabel {
		width: 100%;
		max-width: 90%;
	}

	.prijs-tabel-pakket {
		margin-top: 0;
	}

	#diensten-voetnoot {
		padding-top: 1%;
		padding-bottom: 0;
	}

	#gegevens-tabel {
		width: 65%;
		margin: 10% auto 0;
		font-size: 100%;
	}
	
	#privpol {
		width: 60%;
		margin: 10% auto 0;
	}	

	.contact-formulier {
		margin: 5% auto;
		width: 65%;
	}

	.navigatie-menu {
		word-spacing: 50px;
		margin: 0; 
	}   

	.navigatie-menu a {
		font-size: 140%;
	}

	.info {
		max-width: 90%;
		margin: 8% auto 0;
	}
	
	.info-stuk {
		grid-template-columns: 1fr 3fr;
	}
	
	.info-stuk-nummer {
		font-size: 10vw;
		font-weight: 600;
		padding: 10px 0;
		position: sticky;
		top: 0;
		text-align: center;
		height: calc(10vw + 20px);
		transition: all 0.2s ease-in;
	}
	
	.info-stuk-inhoud {
		border-top: 2px solid #fff;
		display: grid;
		grid-template-columns: 1fr 4fr;
		grid-gap: 10px;
		padding: 15px 0;
	}
	
	.info-stuk-inhoud .info-stuk-titel {
		font-weight: 600;
		max-width: 100%;
	}
	
	footer ul {
    grid-auto-flow: column;
	}
	
	#voetnoot-tabel {
		margin: auto;
	}
	
	.helpdesk{
		display:flex;
	}
	
	#TANKSdesktopClass{
		visibility: visible;
	}

	#TANKSmobileClass{
		visibility: hidden;
		margin-top: 0;
		margin-left: 0;
	}

}

/* kleine aanpassing voor tablets in landscape-mode (er waren complicaties met iPad mini/pro) */
@media (min-width: 768px) and (max-width: 1024px){
	
	.prijs-tabel-pakket {
		margin-top: 30%;
	}
		
	blockquote {
		display: none;
	}

	.logo-achtergrond {
		margin: 10% auto;
		width: 50%;
	}
	
	#index-over {
		margin: 60% auto 10%;
	}
	
	.info {
		margin: 15% auto 0;
	}
	
	#gegevens-tabel {
		width: 65%;
		margin: 15% auto 0;
	}
	
	.helpdesk{
		display: none;
	}
	
	#TANKSdesktopClass{
		visibility: hidden;
	}

	#TANKSmobileClass{
		visibility: visible;
		margin-top: 50%;
		text-align: center;
	}
}



/* scroll wiel verbergen in google chrome, safari, opera, internet explorer, microsoft edge en firefox.*/

html::-webkit-scrollbar{ display: none; }
html { -ms-overflow-style: none; scrollbar-width: none; }
